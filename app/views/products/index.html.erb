<h1>Current Cart:</h1>
<ul>
    <% cart.each do |item| %>
        <li><%= item %></li>
    <% end %>
</ul>

<h1>Add Item:</h1>

<%= form_tag("/add", method: :post) do %>
    <label>New Product:</label>>
    <%= text_field_tag :product %>
    <%= submit_tag "add to cart"%>
<% end %>